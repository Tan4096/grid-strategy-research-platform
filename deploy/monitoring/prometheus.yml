global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "grid-strategy-research-platform-backend"
    metrics_path: /api/v1/metrics
    static_configs:
      - targets: ["backend:8000"]
    http_config:
      headers:
        X-API-Key: "replace-me-admin-key"
